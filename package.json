{
  "name": "novacart-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json for NovaCart deployment",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
